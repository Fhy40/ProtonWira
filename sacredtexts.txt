
$env:FLASK_APP = "main.py" //run this in your working directory

flask run                  //then run this command to start the server

flask run --host=0.0.0.0   //if you want to make the server available to people on the network